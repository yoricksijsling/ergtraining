<h1>Teams</h1>

<table>
  <tr>
    <th>Naam</th>
    <th>Leden</th>
  </tr>

<% @teams.each do |team| %>
  <tr>
    <td><%= link_to team.title, team %></td>
    <td><%= team.members.map{ |m| m.name }.join ', ' %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Team', new_team_path %>
